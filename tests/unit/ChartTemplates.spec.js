import { shallowMount } from '@vue/test-utils';
import ChartTemplates from '../../src/pages/ChartTemplates.vue';
import { mount } from '@vue/test-utils';

describe('ChartTemplates.vue', () =>{
  /*let cmp

  beforeEach(() => {
    cmp = shallowMount(ChartTemplates)
    jest.resetModules()
    jest.clearAllMocks()
  })*/

  it('Setup correctly', () => {
    //const wrapper = mount(ChartTemplates)
    expect(true).toBe(true)
  })

  /*it('checkbox shows and works', () => {
    //const wrapper = mount(Q)
    expect(cmp.contains('checkbox')).toBe(true)
  })*/



})
