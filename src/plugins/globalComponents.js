
/**
 * Register global components here that can be
 * used across the vue app without importing them.
 *
 * For example:
 *   <MyGlobalComponent/>
 */
export default {
  install(Vue) {
    // Vue.component(MyGlobalComponent.name, MyGlobalComponent);
  },
};
