Layouts are intended for use with the vue router
<router-view> elements, that dynamically replace
the content with the component relating to the
current url.

- This specifies the difference between a layout and a page.
